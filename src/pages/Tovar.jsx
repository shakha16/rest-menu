import React from "react";
import phones from "./Home"

export default function Tovar(state) {
    phones.map(item => (
        console.log(item.name)
    ))
    return(
        <div>
            
        </div>
    )
}